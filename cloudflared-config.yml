tunnel: janction-platform
credentials-file: C:\Users\taich\.cloudflared\janction-platform.json

ingress:
  # メインポータル + API + ワークスペース + 管理画面
  - hostname: janction.example.com
    service: http://localhost:3000

  # 管理画面専用ドメイン（オプション）
  # - hostname: admin.janction.example.com
  #   service: http://localhost:3000

  # デフォルトキャッチオール（必須）
  - service: http_status:404
